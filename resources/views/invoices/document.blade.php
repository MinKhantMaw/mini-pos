<div class="mx-auto max-w-2xl bg-white p-6 shadow"><div class="flex justify-between"><div><h1 class="text-2xl font-bold">INVOICE</h1><p>{{ $sale->invoice_number }}</p></div><div class="text-right"><p>{{ $sale->sale_date->format('d M Y') }}</p><p>{{ $sale->customer_name }}</p>@if($sale->customer_phone)<p>{{ $sale->customer_phone }}</p>@endif</div></div><table class="mt-6 w-full text-sm"><thead><tr class="border-b"><th class="py-2 text-left">Item</th><th>Qty</th><th>Price</th><th class="text-right">Amount</th></tr></thead><tbody>@foreach($sale->items as $item)<tr class="border-b"><td class="py-2">{{ $item->product->name }}</td><td class="text-center">{{ $item->quantity }}</td><td class="text-center">{{ number_format($item->selling_price,2) }}</td><td class="text-right">{{ number_format($item->subtotal,2) }}</td></tr>@endforeach</tbody></table><div class="mt-5 ml-auto max-w-xs space-y-1 text-right"><p>Subtotal: {{ number_format($sale->subtotal,2) }}</p><p>Discount: {{ number_format($sale->discount,2) }}</p><p>Delivery: {{ number_format($sale->delivery_fee,2) }}</p><p class="border-t pt-2 text-lg font-bold">Total: {{ number_format($sale->total_amount,2) }}</p></div></div>
