<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">Purchase: {{ $purchase->supplier_name }}</h2></x-slot><div class="p-4 sm:p-6"><div class="bg-white p-5 shadow rounded"><p>{{ $purchase->purchase_date->format('d M Y') }} · {{ $purchase->payment_status }}</p><table class="mt-4 w-full text-sm"><thead><tr class="border-b"><th class="text-left">Product</th><th>Qty</th><th>Cost</th><th>Subtotal</th></tr></thead><tbody>@foreach($purchase->items as $item)<tr class="border-b"><td class="py-2">{{ $item->product->name }}</td><td>{{ $item->quantity }}</td><td>{{ number_format($item->cost_price,2) }}</td><td>{{ number_format($item->subtotal,2) }}</td></tr>@endforeach</tbody></table><p class="mt-4 font-bold">Total: {{ number_format($purchase->total_amount,2) }}</p></div></div></x-app-layout>
