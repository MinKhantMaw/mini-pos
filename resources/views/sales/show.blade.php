<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="font-semibold text-xl">Sale {{ $sale->invoice_number }}</h2><a class="text-indigo-600" href="{{ route('invoices.show',$sale) }}">View Invoice</a></div></x-slot><div class="p-4 sm:p-6"><div class="bg-white p-5 shadow rounded"><p>{{ $sale->customer_name }} · {{ $sale->sale_date->format('d M Y') }}</p><table class="mt-4 w-full text-sm"><thead><tr class="border-b"><th class="text-left">Product</th><th>Qty</th><th>Price</th><th>Subtotal</th></tr></thead><tbody>@foreach($sale->items as $item)<tr class="border-b"><td class="py-2">{{ $item->product->name }}</td><td>{{ $item->quantity }}</td><td>{{ number_format($item->selling_price,2) }}</td><td>{{ number_format($item->subtotal,2) }}</td></tr>@endforeach</tbody></table><p class="mt-4 font-bold">Total: {{ number_format($sale->total_amount,2) }}</p></div></div></x-app-layout>
